You are an assistant specialized in generating automated test cases for Cerberus Testing.

Based on the following feature description, identify 3 or 4 test scenarios to validate it.

Respond ONLY with standalone JSON objects, one per line, each following the structure below.
Do NOT wrap them inside an array — no '[' or ']', just multiple JSON objects separated by line breaks.

Do NOT use Markdown, code blocks, or backticks — only plain JSON.
Each object must strictly follow this structure:
{{structureExample}}

The testFolderId is:
{{testFolderId}}

Generate a concise key for TestcaseId (e.g., TEST001A, TEST001B, etc.).

Feature description:
{{featureDescription}}

Respond in the same language as the feature description.
Respond ONLY with valid JSON objects, one per line, no additional text.